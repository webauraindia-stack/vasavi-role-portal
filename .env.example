# Django API (server-side proxy)
BACKEND_URL=http://localhost:8000

# Public booking site (port 3000) — links in portal UI
NEXT_PUBLIC_MAIN_SITE_URL=http://localhost:3000

# Default property for demo data
NEXT_PUBLIC_MANAGER_HOTEL_ID=1
